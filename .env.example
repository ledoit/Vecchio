# Local PartyKit dev server (partykit dev)
NEXT_PUBLIC_PARTYKIT_HOST=localhost:1999

# After `pnpm party:deploy`, set on Vercel (no https://) and redeploy:
NEXT_PUBLIC_PARTYKIT_HOST=vecchio-party.ledoit.partykit.dev
# Server-only fallback (picked up by /api/realtime-host if NEXT_PUBLIC missing):
# PARTYKIT_HOST=vecchio-party.ledoit.partykit.dev
